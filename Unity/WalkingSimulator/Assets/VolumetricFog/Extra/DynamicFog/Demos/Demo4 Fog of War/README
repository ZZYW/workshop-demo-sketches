This demo scene shows how fog of war works. Basically it’s a masking texture that is used to determine if fog is shown at a specific terrain position or not.

You can call SetFogOfWarAlpha() to specify the alpha value for a zone over the terrain or ResetFogOfWarAlpha() to reset to normal.